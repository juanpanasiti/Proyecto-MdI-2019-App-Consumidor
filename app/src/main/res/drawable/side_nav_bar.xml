<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:centerColor="@color/menu"
        android:endColor="@color/menu"
        android:startColor="@color/menu"/>
</shape>
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.changosconsumidor">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AgregarItem"></activity>
        <activity android:name=".DeleteModifyProduct" />
        <activity android:name=".DeleteModifyCategory" />
        <activity android:name=".ListaActivity" />
        <activity android:name=".ProductosActivity" />
        <activity android:name=".CategoriasActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".NewCategory" />
        <activity android:name=".Home" />
        <activity android:name=".CategoryActivity" />
        <activity android:name=".Registro" />
        <activity android:name=".PantallaDeMuestra" />
        <activity android:name=".MainRegistro" /> <!-- suppress AndroidDomInspection -->
        <activity
            android:name=".SplashScreen"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_splash_screen"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>